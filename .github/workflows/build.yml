name: Build and Push Docker Images

on:
  workflow_dispatch:
  push:
    branches:
      - 'Master'
    paths:
      - Dockerfile
  schedule:
    - cron: '0 8 * * TUE'

jobs:
  build_and_push:
    name: Build and Push Docker Image
    runs-on: ubuntu-latest
    env:
      LATEST_VERSION: v1.12.1.0 # Determines :latest tag
    strategy:
      fail-fast: false
      matrix:
        version:
          - v1.11.11.0
          - v1.12.0.0
          - v1.12.1.0
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Set up QEMU
        uses: docker/setup-qemu-action@v3

      - name: Login to GitHub Container Registry
        uses: docker/login-action@v3
        with:
          registry: ghcr.io
          username: ${{ github.repository_owner }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Prepare tags
        id: prep_tags
        shell: bash
        run: |
          if [[ "${{ matrix.version }}" == "${{ env.LATEST_VERSION }}" ]]; then
            TAGS="ghcr.io/lingyu-ily/blueprint-tc:latest,ghcr.io/lingyu-ily/blueprint-tc:${{ matrix.version }}"
          else
            TAGS="ghcr.io/lingyu-ily/blueprint-tc:${{ matrix.version }}"
          fi
          echo "VERSION_TAG=${{ matrix.version }}" >> $GITHUB_ENV
          echo "TAGS=$TAGS" >> $GITHUB_ENV

      - name: Build and Push Docker Image
        uses: docker/build-push-action@v5
        with:
          context: .
          file: ./Dockerfile
          platforms: linux/amd64,linux/arm64
          push: true
          tags: ${{ env.TAGS }}
          build-args: VERSION_TAG=${{ env.VERSION_TAG }}
